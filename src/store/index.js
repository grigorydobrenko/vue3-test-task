import { createStore } from 'vuex'
import {layoutModule} from "@/store/layoutModule";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    layout: layoutModule
  }
})
